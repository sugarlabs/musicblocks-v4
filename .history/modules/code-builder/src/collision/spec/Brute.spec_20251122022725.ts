import { describe, it, expect } from 'vitest';
import { Brute } from '../Brute';

describe('Brute Collision', () => {
    it('should detect collision correctly', () => {
        // Example: call Brute collision function
        const result = Brute.detectCollision(
            {
                /* sample input */
            },
            {
                /* sample input */
            },
        );
        expect(result).toBe(true); // or false based on expected
    });
});
