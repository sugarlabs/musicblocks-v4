export default class DropDownPalette {
    // List of advance category (fetched from engine)

    // Block corresponding to advance category(fetched from engine)

    selectedCategory: string | null;

    constructor() {
        this.selectedCategory = null;
    }
}
